class DatabaseConfig {
  static const String host = 'localhost'; // Use this for Android emulator to connect to localhost
  // Use 'localhost' for iOS simulator or physical device connection through proper networking
  static const int port = 3306;
  static const String user = 'root';
  static const String password = '';
  static const String db = 'flutter_user_db';
}